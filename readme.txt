First you can compile the lab by running "make" in the current folder

Then once the program is ready it can be run using the following

./lab4main (input file name) (output file name)

Included is a sample file in the proper format titled "class_records"